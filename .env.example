# Base URL for the GoldRisk API. Not yet in use — no backend exists in this
# phase. Copy this file to .env.local and set a value once a backend is
# available.
VITE_API_BASE_URL=http://localhost:3000/api
